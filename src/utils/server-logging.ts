/**
 * Server-side logging utilities
 */

// Note: migrateLogging has been removed as modifying global console is an anti-pattern
// Use the structured logger from '@/lib/monitoring/logger' instead

/**
 * Initialize server-side logging
 * Currently a no-op, but kept for potential future server-specific logging setup
 */
export function initializeServerLogging() {
  // Future server-specific logging initialization can go here
}
