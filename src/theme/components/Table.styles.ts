import { mode } from '@chakra-ui/theme-tools';

const Table = {
  variants: {
    simple: {
      th: {
        borderColor: 'ipd.border',
      },
      td: {
        borderColor: 'ipd.border',
      },
    },
  },
};

export default Table;
