BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[saved_prior_art] ADD [claim1] NVARCHAR(max),
[summary] NVARCHAR(max);

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
